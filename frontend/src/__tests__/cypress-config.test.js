// import config from "../../cypress.config.js";

// test("Cypress config loads", () => {
//   expect(config).toBeDefined();
// });

describe("SonarCloud frontend dummy test", () => {
    test("dummy ok", () => {
      expect(true).toBe(true);
    });
  });
  