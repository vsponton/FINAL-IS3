# Control de versiones
.git
.gitignore
.github

# Variables de entorno
.env
.env.*

# Dependencias del host (se instalan dentro del contenedor con npm ci)
node_modules

# Build previo del host (se regenera en el contenedor)
dist

# Cobertura y resultados de test
coverage
test-results

# Tests unitarios y e2e (no necesarios para el build de producción)
src/__tests__
cypress

# Archivos temporales y de sistema
*.md
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
